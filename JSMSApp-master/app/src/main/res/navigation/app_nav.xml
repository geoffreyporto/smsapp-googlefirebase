<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/app_nav">

    <!--fragment
        android:id="@+id/citas"
        android:name="com.daa_lm.app_consultorio.fragments.fragmentoCita"
        android:label="fragment_citas"
        tools:layout="@layout/fragment_citas" >
        <action
            android:id="@+id/action_confirmar"
            app:destination="@id/confirmarCita" />
    </fragment-->

    <fragment
        android:id="@+id/confirmarCita"
        android:name="com.daa.jsmsapp.ui.ConfirmationAppointment"
        android:label="fragment_confirmar_cita"
        tools:layout="@layout/activity_confirmation_appointment" >
        <argument
            android:name="Doctor"
            app:argType="string"
            android:defaultValue="N/A" />
        <argument
            android:name="Hospital"
            app:argType="string"
            android:defaultValue="N/A" />
        <argument
            android:name="Especialidad"
            app:argType="string"
            android:defaultValue="N/A" />
        <argument
            android:name="Dia"
            app:argType="string"
            android:defaultValue="N/A" />
        <argument
            android:name="Horario"
            app:argType="string"
            android:defaultValue="N/A" />
        <!--action
            android:id="@+id/action_regresar_inicio"
            app:destination="@id/inicio" /-->
        <action
            android:id="@+id/action_confirmar"
            app:destination="@id/confirmarCita" />
    </fragment>


    <!--fragment
        android:id="@+id/inicio"
        android:name="com.daa_lm.app_consultorio.fragments.fragmentoInicio"
        android:label="fragment_inicio"
        tools:layout="@layout/fragment_inicio" /-->

</navigation>